# ===================================================
# INVOICE LANGGRAPH AGENT - BACKEND DEPENDENCIES
# ===================================================

# === CORE FRAMEWORK ===
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0          # Checkpoint base classes
langgraph-checkpoint-sqlite>=2.0.0   # SQLite checkpoint support
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0          # Claude integration for non-deterministic nodes
 
 # === API LAYER ===
 fastapi>=0.115.0
 uvicorn[standard]>=0.32.0
 sse-starlette>=2.1.0                # Server-Sent Events for real-time logs
 python-multipart>=0.0.12            # File upload support
 
 # === VALIDATION ===
 pydantic>=2.9.0
 pydantic-settings>=2.5.0            # Settings management
 
 # === DATABASE ===
 sqlalchemy>=2.0.0
 aiosqlite>=0.20.0                   # Async SQLite support
 
 # === LOGGING ===
 loguru>=0.7.0                       # Structured logging with colors
 
 # === MOCKING & DATA GENERATION ===
 faker>=30.0.0                       # Realistic mock data
 
 # === UTILITIES ===
 python-dotenv>=1.0.0                # Environment variable loading
 httpx>=0.27.0                       # Async HTTP client
 orjson>=3.10.0                      # Fast JSON serialization
 tenacity>=9.0.0                     # Retry logic
 
 # === TESTING ===
 pytest>=8.3.0
 pytest-asyncio>=0.24.0
 pytest-cov>=5.0.0                   # Coverage reporting
 
 # === LINTING (Dev) ===
 ruff>=0.6.0                         # Fast Python linter

